# Memory Index

<!-- Auto-initialized from global memory. Add project-specific entries below. -->

- [agent-repo-root-resolution](feedback_agent_repo_root_resolution.md) — Automated agents fed relative file lists must resolve the repo root from cwd first — never guess /home/user/… paths; working-dir denials are path-based, don't retry with command variants (inherited from global)
- [Application code test discipline (Vitest + Playwright)](feedback_app_code_test_discipline.md) — Every slice that ships application code must include automated tests for the new behavior. Santa-method dual-review is not a substitute. Stack defaults Vitest + Playwright + Neon branching for Next.js + Prisma projects. (inherited from global)
- [AskUserQuestion requires a non-empty questions array](feedback_askuserquestion_requires_questions.md) — AskUserQuestion fails with InputValidationError unless the required `questions` array param is present and non-empty. (inherited from global)
- [feedback_auth_outage_not_script_failure](feedback_auth_outage_not_script_failure.md) — "A circuit breaker over a headless `claude -p` call must read STDOUT too — entitlement/auth/quota rejections print there, and counting them as failures disables a working phase" (inherited from global)
- [bash32-heredoc-cmdsubst](feedback_bash32_heredoc_cmdsubst.md) — On macOS /bin/bash 3.2, never nest a here-doc inside $(...) when building LLM prompts — write to a temp file instead (inherited from global)
- [c-locale-byte-slice-breaks-grep](feedback_c_locale_byte_slice_breaks_grep.md) — Bash `${var:0:N}` slices by BYTES under LC_CTYPE=C — truncating CJK text mid-UTF-8-sequence corrupts the output file, after which BSD grep silently matches NOTHING in it (rc=1 on strings that are present) (inherited from global)
- [Check existing infrastructure before building parallel systems](feedback_check_existing_before_building.md) — Before designing new backup/logging/scheduling/caching, audit ~/.claude for whether the need is already solved (inherited from global)
- [claude-skill-roots-separator-mismatch](feedback_claude_skill_roots_separator.md) — CLAUDE_SKILL_ROOTS separator must match between settings.json (':') and every consumer script — a ';'-only split makes business-ops skills invisible to that tracker (inherited from global)
- [communication-language-and-simplicity](feedback_communication_language.md) — Default to the user's working language (usually Chinese) and the simplest wording; frame decisions as options + one-line recommendation + one-line reason; keep code/terms/paths in original (inherited from global)
- [Decision-asking discipline (plain language + why + why-now + industry standard + recommendation)](feedback_consult_industry_standard.md) — When asking the user to decide ANYTHING non-trivial, frame the ask with five facets — plain-language summary, why it matters, why now, what industry does, and a concrete recommendation with reasoning. Never throw bare open questions. (inherited from global)
- [dash-prefixed-paths-need-guard](feedback_dash_prefixed_paths_need_guard.md) — Paths starting with `-` (e.g. ~/.claude/projects/-Users-ac-Projects/) get parsed as CLI options — always use `--` or `./` prefix; with 2>/dev/null the failure becomes a silent wrong answer (inherited from global)
- [Never propose deleting a directory whose contents you have not enumerated](feedback_deletion_proposals_need_inventory.md) — Disk-cleanup recommendations must list what is actually inside each candidate path first; "looks like cache/junk" is not evidence, and the user has had to rescue real working assets from such proposals (inherited from global)
- [deliverable-explicit-paths](feedback_deliverable_paths.md) — Every deliverable mentioned in a final reply gets its explicit absolute path (folder + filename) — user must never hunt for outputs (inherited from global)
- [destructive-cleanup-not-compound](feedback_destructive_cleanup_compound.md) — Never bundle destructive cleanup (pkill / rm -rf) into compound Bash commands with other steps — the user denies the WHOLE compound; issue minimal standalone destructive calls (or mv to Trash) (inherited from global)
- [docs-to-mechanism](feedback_docs_to_mechanism.md) — "When adding a discipline rule, ask if it can be *physically enforced* (slash command / shell alias / symlink / hook) before settling for *documented intent* (feedback file / CLAUDE.md prose). Mechanism wins; docs rot." (inherited from global)
- [Editing discipline — verify before deleting](feedback_editing_discipline.md) — Mistakes made deleting CLAUDE.md content that appeared redundant but wasn't — rules for safe editing (inherited from global)
- [Execute the literal ask before any adjacent exploration](feedback_execute_the_literal_ask.md) — When the user names a specific deliverable, build exactly that first — do not substitute testing/exploring adjacent cases (more companies, more styles, more sites) for the requested work (inherited from global)
- [feedback-fable-orchestrator-only](feedback_fable_orchestrator_only.md) — "Fable must NOT execute tasks directly — always orchestrate cheap-model agent fleets (sonnet/opus/haiku) for execution, parallel where possible" (inherited from global)
- [find-delete-needs-maxdepth](feedback_find_delete_needs_maxdepth.md) — danger-block hook blocks `find ... -delete` on non-tmp paths unless -maxdepth is present — add -maxdepth or pipe to mv-trash (inherited from global)
- [gate-exit127-tool-missing](feedback_gate_exit127_tool_missing.md) — In a deterministic verify gate, exit 127 / "no such file or directory: node_modules/.bin/<tool>" means the TOOL is missing, not that the code failed — never report it as RED or let it trigger a tier escalation (inherited from global)
- [hook-path-guard-must-anchor-repo-prefix](feedback_hook_path_guard_repo_prefix.md) — PostToolUse/path-matching guards must anchor on the repo PREFIX ("$REPO/"*), not a bare */.claude/* substring — per-project .claude/ dirs also match and cause "git add ... outside repository" fatals (inherited from global)
- [image-magic-bytes-ftyp-is-heic](feedback_image_magic_bytes_heic.md) — A downloaded "image" whose magic bytes are `...ftyp` is a valid HEIC/AVIF/ISOBMFF container, not a failed/login-page download — convert it, don't re-download (inherited from global)
- [long-bash-needs-background](feedback_long_bash_needs_background.md) — Foreground Bash hard-caps at 10m (default 2m) — batch/scrape/audit commands estimated over ~2 min must use run_in_background:true or chunk, or they die with exit 143 mid-work (inherited from global)
- [memory-promotion-cleanup](feedback_memory_promotion_cleanup.md) — When promoting a feedback_*.md from project-local to global, audit ALL project memory dirs for stale duplicates — not just the originating project (inherited from global)
- [feedback_never_auto_escalate_fable](feedback_never_auto_escalate_fable.md) — Never auto-escalate the main model up to Fable; ceiling is Opus. Delegating DOWN is fine; Fable is explicit-choice only. (inherited from global)
- [feedback_new_agent_needs_session_restart](feedback_new_agent_needs_session_restart.md) — "A newly written ~/.claude/agents/*.md is NOT callable as subagent_type in the session that created it — run it via general-purpose + 'adopt this file as your instructions'" (inherited from global)
- [no-dead-infra](feedback_no_dead_infra.md) — "Extract-only or defer when there's no clean live caller/consumer; don't wire speculative infra" (inherited from global)
- [No fake metrics — measure, don't estimate](feedback_no_fake_metrics.md) — When a measurement can be obtained by actually running the audit/script, run it. Never compute/estimate and present as measured. (inherited from global)
- [no-foreground-sleep-to-wait](feedback_no_foreground_sleep.md) — The harness BLOCKS foreground `sleep N && <cmd>` in Bash — use an `until <check>; do sleep 2; done` loop, Monitor, or run_in_background instead of sleeping to poll (inherited from global)
- [offload-mode must be visible in the reply](feedback_offload_visibility.md) — When offload-mode is ON, name the vendor + tier for each delegated step (or say plainly that nothing was offloaded) — the flag gives the user no feedback otherwise, and silent non-use reads as wasted quota (inherited from global)
- [perl-alarm-timeout-unenforced](feedback_perl_alarm_timeout_unenforced.md) — On macOS (no GNU `timeout`), the `perl -e 'alarm shift; exec @ARGV'` fallback does NOT reliably kill a Node-based child (`claude -p`) — caps silently fail; install coreutils `gtimeout` or kill the process group (inherited from global)
- [pin-mcp-server-versions](feedback_pin_mcp_server_versions.md) — Always version-pin uvx/npx-launched MCP servers in project .mcp.json — unpinned = supply-chain RCE on repo open (inherited from global)
- [pnpm is the package manager — never npm, never mixed](feedback_pnpm_is_the_package_manager.md) — Every JS/TS project on this machine uses pnpm; do not run `npm install`/`npx` for project deps, and when auditing disk usage flag npm-managed node_modules as a migration target rather than proposing deletion (inherited from global)
- [Verification probes must be read-only — verify write paths on a dummy fixture](feedback_probe_must_be_readonly.md) — A probe run "just to check it doesn't error" must never touch a control that mutates live state; to verify a write path, create a throwaway fixture and act on that instead. (inherited from global)
- [python-heredoc-literals](feedback_python_heredoc_literals.md) — Python embedded in bash heredocs/`-c` must use Python literals (True/False/None) and avoid backslash-escaped quotes inside f-string expressions (inherited from global)
- [Read before Edit — tool precondition](feedback_read_before_edit.md) — Must use Read tool (in current session) before Edit OR Write on any existing file. A prior Write does NOT satisfy this — only Read does. (inherited from global)
- [reference-image-artifacts-propagate](feedback_reference_image_artifacts.md) — AI image gen (NBP/Gemini) faithfully copies defects from reference images — artifact-check every synthetic swatch/anchor before feeding it in (inherited from global)
- [rename-blast-radius](feedback_rename_blast_radius.md) — Naming rules for ~/Projects/ live in ~/Documents/PERSONAL_SYSTEM/NAMING_CONVENTION.md Part 2 — read it before any dir/repo/skill rename, count references first, and prove 0 live hits of the old name afterwards (inherited from global)
- [feedback-resource-cleanup](feedback_resource_cleanup.md) — Every long-running spawn (local server / tunnel / browser / test runner) needs a documented cleanup. Background-with-no-cleanup is forbidden. List-only audit via process-leak-check.sh. (inherited from global)
- [santa-method auto-routes to Tier 4 on high-stakes signals + Codex follow-up](feedback_santa_routing.md) — When user invokes santa with any of {ultrathink, plan, best way, ship, production, important...}, default to Tier 4 (cross-model + 2 Opus + merger). After Tier 4 PASS, ALWAYS surface /codex:adversarial-review prompt to user. (inherited from global)
- [scrape-autoclose](feedback_scrape_autoclose.md) — "Any scraping/browser-automation task MUST auto-close its browser/server when done (trap EXIT, not end-of-script) — user's repeated resource-waste concern" (inherited from global)
- [shell-syntax-platform-match](feedback_shell_syntax_platform_match.md) — Match shell command syntax to the actual shell/OS — no PowerShell idioms on bash, no GNU coreutils long-flags (mktemp --suffix, sed -i, date -d) on macOS BSD, and no bash word-splitting assumptions on zsh (unquoted $VAR in for-loops doesn't split) (inherited from global)
- [skill-triggers-audit-parsing-pitfalls](feedback_skill_triggers_audit_parsing.md) — The nightly skill-triggers audit's "below target" count is inflated by two implementation pitfalls — YAML block-scalar descriptions and an over-literal negation matcher (inherited from global)
- [Small atomic slices by default — earn the right to batch](feedback_small_steps_default.md) — For multi-step projects, propose small reviewable slices unless user explicitly authorizes batching (inherited from global)
- [StructuredOutput takes the schema object itself, not a wrapper](feedback_structured_output_no_double_wrap.md) — Subagents forced into a structured schema double-wrap the payload (`{"findings": {"findings": []}}`) — pass the schema's own top-level object, unnested. (inherited from global)
- [system-python-has-no-numpy](feedback_system_python_no_numpy.md) — Homebrew system python3 (3.14) has NO numpy — pixel-math one-liners must use PIL-only math, check the import first, or run inside a project venv (inherited from global)
- [Test core files before shipping](feedback_test_before_ship.md) — Every change to automation scripts, hooks, or config files must be tested for syntax + functional correctness before being considered done (inherited from global)
- [todowrite-json-parse-failures](feedback_todowrite_json_multibyte.md) — TodoWrite AND AskUserQuestion intermittently fail InputValidationError ("could not be parsed as JSON") on long Chinese-content payloads — don't retry identical input; shorten/split (inherited from global)
- [Verify before claiming — never pattern-match unknown tools to known ones](feedback_verify_before_claiming.md) — When the user introduces a new tool/service/API I don't know for certain, read docs / source / run dry test before making claims about cost, behavior, or dependencies (inherited from global)
- [feedback-workflow-model-tiering](feedback_workflow_model_tiering.md) — "Workflow/fan-out subagents default to inheriting the main-loop model — if that's Fable, a 30+ agent vision batch runs entirely on Fable = very expensive. Explicitly pin model per agent." (inherited from global)
- [workflow-tool-has-no-run-in-background](feedback_workflow_no_run_in_background.md) — The Workflow tool always runs in background and rejects a run_in_background param — passing it throws InputValidationError (inherited from global)
- [Wrap reusable runbooks in skills — no memorization debt](feedback_wrap_interfaces_as_skills.md) — Any runbook / prompt / tool that will be invoked repeatedly must have a natural-language skill wrapper before being "done" (inherited from global)
- [zsh-glob-nomatch-aborts](feedback_zsh_glob_nomatch.md) — In zsh an unmatched glob aborts the ENTIRE command ("no matches found") — guard multi-glob listings with (N) or existence checks (inherited from global)
- [zsh-lowercase-path-clobbers-PATH](feedback_zsh_lowercase_path_clobbers_PATH.md) — In zsh, the lowercase variable `path` is bound to `PATH` — using it as a loop/temp variable silently destroys PATH and every later command dies with "command not found" (inherited from global)

